# SHOW BOOKS
def show_books():
    if len(books) == 0:
        print("No books available")
    else:
        print("Books List:")
        for name, status in books.items():
            print(name, "->", status)
